// Declared & Assigned
const cityNames = ['kodad','khammam','suryapet','hyderabad','vizag'];
console.log(cityNames[1].toUpperCase());

for(let i=0;i<cityNames.length;i++){
    console.log(cityNames[i].toUpperCase());
    console.log(i);
}